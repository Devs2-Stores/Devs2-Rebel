<header class="password-header">
  <div class="password-header__inner">
    <a href="{{ routes.root_url }}" class="password-header__logo" title="{{ shop.name }}">
      {{ shop.name }}
    </a>
  </div>
</header>

{% schema %}
{
  "name": "Password header",
  "tag": "div",
  "class": "section-password-header"
}
{% endschema %}

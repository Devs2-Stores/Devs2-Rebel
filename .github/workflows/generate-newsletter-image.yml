name: Generate Newsletter Image via BFL API

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled for release-readiness stabilization
        run: |
          echo "BFL newsletter image generation is disabled for this release."
          echo "Restore the removed generator in a separate plan if generation is required again."

name: Newsletter image workflow (retired)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled for release-readiness stabilization
        run: |
          echo "Newsletter image generation is retired. Use ai-image-studio with Wan."

---
title: "Phase 1 Baseline Validation"
created: 2026-06-29
phase: 1
status: completed
---

# Phase 1 Baseline Validation

## Summary

Baseline validation confirms the audit findings are still accurate. The theme is not release-ready yet: `shopify theme check` still fails with 3 errors, the BFL workflow still references a deleted script, and quickview/search runtime files remain in an unresolved migration state.

## Commands Run

| Check | Result |
|---|---|
| `git status --short --untracked-files=all` | Dirty working tree with modified assets/sections/templates, large section prune/move set, deleted generator script, untracked plan/report files, and untracked quickview runtime files. |
| `git diff --stat HEAD` | 65 files changed, 1,475 insertions, 10,837 deletions. |
| `shopify theme check` | Failed: 3 errors, 2 warnings. |
| JSON validation with Shopify header stripping | Passed: 18 JSON files checked, 0 parse errors. |
| Active template/section reference scan | Passed: 53 section refs checked, 0 missing section refs. |

## Working Tree Classification

### Intentional / expected for this work session

- New plan files under `plans/260629-0000-release-readiness-stabilization/`.
- Existing audit report at `plans/reports/260628-devs2-shopify-theme-audit.md`.

### Existing migration/prune state to preserve until implementation decisions

- Large section prune/move set from `sections/*.liquid` into `backup/sections-pruned-2026-06-20/sections/`.
- `backup/sections-pruned-2026-06-20/MANIFEST.md` added/deleted state still needs final git hygiene before release.
- Modified active storefront files across `assets/`, `sections/`, `snippets/`, `templates/`, and `config/settings_data.json`.

### Runtime files requiring care

- `sections/main-search-quickview.liquid` appears both as moved to backup and recreated as untracked. Treat as an active quickview/search candidate until Phase 2/3 inventory proves otherwise. Do not delete blindly.
- `templates/product.quickview.liquid` is untracked but referenced by the quickview direction in the audit. Treat as an active candidate until Phase 2/3 aligns quickview contracts.
- `tools/generate_newsletter_image.py` is deleted. Validation decision from plan: disable/remove the BFL/newsletter workflow for this release; do not restore the script unless the user reverses that decision.

## Theme Check Baseline

```text
140 files inspected
5 offenses across 4 files
3 errors
2 warnings
```

Errors:

- `sections/banner-hotspots.liquid:28` — `ImgWidthAndHeight`, raw custom URL `<img>` missing width/height.
- `sections/image-text-overlay.liquid:19` — `ImgWidthAndHeight`, mobile raw custom URL `<img>` missing width/height.
- `sections/image-text-overlay.liquid:33` — `ImgWidthAndHeight`, desktop raw custom URL `<img>` missing width/height.

Warnings:

- `sections/main-blog.liquid:460` — hardcoded `/collections/all`; should use `routes.all_products_collection_url`.
- `snippets/product-card-special.liquid:1` — orphaned snippet.

## JSON / Section Reference Baseline

- JSON files checked: 18 (`templates/*.json` + `sections/*-group.json`).
- JSON parse errors after stripping Shopify autogenerated header comments: 0.
- Section references checked: 53.
- Missing active section references: 0.

This means active JSON templates and section groups are coherent with the currently active `sections/*.liquid` files, despite the large prune/move state.

## Phase 2 Blocker List

1. Fix theme-check image dimension errors while keeping raw `custom_image_url` compatibility.
2. Stabilize quickview request lifecycle and data/selector contract.
3. Remove `view=smart` dependency from search fallback or restore a valid active fallback contract.
4. Disable/remove BFL/newsletter workflow that calls deleted `tools/generate_newsletter_image.py`.
5. Clean stale `.theme-check.yml` references after real fixes, not by suppressing current errors.

## Open Risks

- Shopify preview was not run in this phase; it remains a blocking Phase 5 gate.
- Working tree is very dirty; release hygiene must be checked again after every implementation phase.
- Quickview runtime files are not yet fully classified as canonical vs accidental duplicate; Phase 2/3 must inventory before deleting or consolidating.
